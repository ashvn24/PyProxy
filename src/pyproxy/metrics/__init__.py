"""Prometheus metrics collection and export.

Collects and exposes operational metrics (request count, latency
histograms, connection gauges, upstream status) in Prometheus
exposition format.

Implemented in Phase 12+.
"""
